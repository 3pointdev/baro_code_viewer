import { useEffect } from "react";

export default function Home() {
  useEffect(() => {}, []);

  return (
    <article className="text-bg-gray-900 bg-gray-300 dark:bg-gray-800 dark:text-white w-screen min-h-screen h-full"></article>
  );
}
